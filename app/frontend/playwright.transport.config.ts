import {defineConfig} from '@playwright/test';
export default defineConfig({testDir:'./e2e',testMatch:'transport.spec.ts',timeout:30000,workers:1,reporter:'list',use:{baseURL:'http://127.0.0.1:3178',channel:'chrome'},projects:[320,390,768,1440].map(width=>({name:`width-${width}`,use:{viewport:{width,height:900}}})),webServer:{command:`node node_modules/vite/bin/vite.js preview --config vite.food-test.config.ts --outDir "${process.env.TRANSPORT_BUILD_DIR||'dist'}" --host 127.0.0.1 --port 3178 --strictPort`,url:'http://127.0.0.1:3178',reuseExistingServer:false}});
