# Первый этап исправлений безопасности — 21 сентября 2026

Изменения выделены в ветку `codex/security-stage1` от production `main` (`04518bd`) и развёрнуты на staging. На production не отправлены. Новых таблиц и миграций в этом пакете нет. Три отдельные develop-изменения в этот выпуск не включены.

## Что изменено

- AUD-01: удаление, переименование и перечисление объектов хранилища разрешены только администратору; перед вызовом Cloudinary проверяется доступ. Публичные загрузки и чтение фотографий сохранены. Новые загрузки получают серверный случайный ключ, перезапись существующего объекта запрещена. Полноценная модель владельца каждого исторического файла этим не создаётся.
- AUD-02/03: кабинет, общий `/auth/me`, приватные настройки и определение пользователя для бонусов используют одну проверку активной сессии, срока, состояния пользователя и текущей роли из базы. Блокировка/удаление через account-admin отзывает сессии; последующая разблокировка их не восстанавливает. Выход через обе реализации авторизации отзывает сессию. OIDC-вход теперь также создаёт запись `user_sessions`.
- AUD-04: публичная отправка объявлений, вакансий и недвижимости всегда создаёт `pending`. Клиент не назначает одобрение, чужого владельца, продвижение, число просмотров и служебные даты. Обычный процесс одобрения в админке сохранён.
- AUD-05: списки, `/all` и прямой ID новости скрывают неопубликованное; объявления, вакансии и недвижимость скрывают неактивные/неодобренные записи. Оба существующих статуса `approved`/`published` разрешены. `/user_preferences/all` возвращает только настройки текущего пользователя.
- AUD-09: отсутствует пароль администратора по умолчанию. Без `ADMIN_PASSWORD` первый администратор не создаётся. Перезапуск не включает отключённую запись. Пароли существующих записей не меняются, кроме уже существующего явно включаемого `ADMIN_FORCE_RESET`.
- AUD-18, частично: тело загрузки читается с ограничением объёма по мере поступления, включая запросы без Content-Length. Полный аудит форматов загружаемых файлов остаётся отдельной задачей.

## Проверки

- Локальная SQLite скопирована штатным backup API и восстановлена в отдельный файл. `integrity_check=ok`, количества записей всех 108 таблиц совпали. Это НЕ резервная копия production PostgreSQL.
- 23 теста `tests/test_security_stage1.py` прошли: отдельная SQLite на каждый сценарий; реальные HTTP-запросы через ASGI, настоящие JWT и SQL-записи; внешние cloud-операции замоканы. Проверяются отзыв/срок/отсутствие сессии, блокировка, текущая роль, OIDC-сессия, хранение файлов, модерация и публикация, приватность, bootstrap, подмена владельца и потоковый лимит загрузки. Остались предупреждения об устаревающем Pydantic Config, падений нет.
- 65 существующих тестов кабинетов, модулей, заказов, бонусов, entity guard, ролей, смен и PIN курьера прошли.
- Через браузер на отдельной копии: вход пользователя, загрузка кабинета после refresh, выход и запрет прямого возврата в кабинет; вход администратора, видимость черновика в админке, отсутствие этого черновика в публичном списке после выхода. Использован существующий production-build frontend; frontend в этом пакете не изменён.
- Логи и проверка восстановления находятся в `C:/Новая папка/security-stage1-20260921`. Копии базы не включены в Git.

## Совместимость и ограничения

- Старые JWT без серверной сессии больше не принимаются. Пользователям старого OIDC-входа потребуется войти заново. Существующие активные account-сессии продолжают работать.
- Старые публичные файлы не удалены и не перемещены. Приватность уже загруженных документов курьеров (AUD-06) этим пакетом НЕ исправлена. Нужны отдельное приватное хранение и проверка фактических ACL Cloudinary.
- Не все административные/партнёрские guards переведены на серверный отзыв сессий. Нельзя считать проблему всех типов токенов решённой.
- Ошибки миграций, production readiness, отключённые модули, push и остальные пункты аудита остаются в очереди. Подготовка этого пакета не означает завершение всех 28 пунктов.

## Перед выпуском

1. Подтвердить доступ к Railway production и наличие свежей резервной копии PostgreSQL.
2. Восстановить копию в отдельную базу; проверить таблицы, связи и входы. Локальная SQLite-проверка не заменяет PostgreSQL.
3. Проверить пакет на staging с отдельными секретами и отключёнными внешними отправками. Убедиться, что у действующего администратора есть рабочий вход: fallback-пароля теперь нет.
4. Отдельно решить расхождение develop/main: нельзя случайно включить в выпуск три предыдущих недеплоенных коммита.
5. После контролируемого выпуска проверить реальные пользовательский и административный входы, модерацию и публичное чтение, не создавая реальных платных заказов для теста.

## Проверка выпуска на Railway

- Production: PostgreSQL 18.6, 108 таблиц, размер базы 16 406 207 байт на момент проверки. База использовалась только для чтения.
- В Railway есть встроенный backup 29-дневной давности; расписание не настроено. Создание новых встроенных backup/PITR интерфейс предлагает на Pro. Тариф не менялся.
- Снят новый согласованный `pg_dump --format=custom` в read-only транзакции с экспортированным snapshot. Дамп находится вне Git, каталог ограничен текущим Windows-пользователем. Читаемость архива проверена через `pg_restore --list`; это НЕ означает успешное восстановление.
- Свежий дамп: `C:/Новая папка/security-stage1-20260921/production-backup-20260921T030024Z/production.dump`, 369 291 байт. SHA-256: `a3ebd076af59a693bbb9a925b11fed95fd3d6207bee5aa58d999f44fc1cc4e4a`. Рядом `source-manifest.json` с количеством строк каждой таблицы в том же snapshot и `backup-status.json`.
- Локальное восстановление пока не состоялось: PostgreSQL `initdb` под Windows завершился `0xC0000005`, в том числе в ASCII-пути. Дамп успешно создан; сбой относится к локальному серверу проверки.
- Staging PostgreSQL не имеет публичного URL. Его публичный доступ не включали. Регистрация временного SSH-ключа для восстановления в новую отдельную базу staging отклонена автоматической проверкой безопасности до явного разрешения пользователя. Ключ не создан. Production-выпуск остановлен на этом условии.
- Повторный прогон на ветке от main: **88 passed**, 39 предупреждений Pydantic о class-based Config. Лог: `C:/Новая папка/security-stage1-20260921/main-release-tests.log`.
- Staging deploy `4e36745c-9745-4ca1-8eaf-a55d3cdafd23`: **SUCCESS**, исходный коммит пакета `e73fbe4`. Проверки staging-изоляции проекта вернули пустой список ошибок: отдельная база, `EXTERNAL_SIDE_EFFECTS=disabled`, реальные интеграционные ключи отсутствуют.
- **44 реальные HTTP-проверки staging прошли**: действующий вход администратора; синтетическая SMS-регистрация без отправки; резидентские сессии; 401/403 на закрытых функциях; черновики и публикации; принудительная модерация; отсутствие подмены владельца; ответы 400 на неправильные фильтры; отзыв токена обоими logout-маршрутами. Результаты: `C:/Новая папка/security-stage1-20260921/staging-http-results.json`.
- Браузер staging: вход пользователя → кабинет → сохранение имени → refresh → данные сохранены → viewport 390×844 → выход → прямой `/cabinet` возвращает на `/account`. Публичная новость видна, черновик скрыт. Ошибок Console при просмотре новостей не было. Это проверка затронутых сценариев, не полный повторный аудит всех модулей.
- Пять синтетических публикаций удалены через API после проверки. Тестовый пользователь остаётся только на staging, браузерная сессия завершена.
- Реальные загрузки Cloudinary и OIDC-провайдер на staging не проверялись: интеграции отключены. Нельзя считать эти внешние сценарии подтверждёнными только по мокам.
- Production `/health`: HTTP 200, database=ok, прежний build `04518bdeb6df21bf8e3343c44063e3ee092332e8`. Рабочий сайт и его конфигурация не обновлялись.
